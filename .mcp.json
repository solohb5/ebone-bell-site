{
  "mcpServers": {
    "firecrawl": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}"
      }
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@context7/mcp-server"]
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-sequential-thinking"]
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      }
    }
  },
  "_comments": {
    "overview": "MCP configuration for Eboné F. Bell website project",
    "why_this_config": "Essential 5 MCPs for development velocity",
    "essential_5": {
      "firecrawl": "🔥 CRITICAL - Web scraping & content extraction (requires API key)",
      "github": "🐙 CRITICAL - Git automation & PR creation (requires token)",
      "context7": "📚 HIGH VALUE - Current API docs, prevents hallucination",
      "sequential-thinking": "🧠 HIGH VALUE - Complex reasoning & planning",
      "brave-search": "🔍 HIGH VALUE - Real-time web search (requires API key)"
    },
    "optional_add_later": {
      "puppeteer": "Browser automation for E2E testing - npx -y @modelcontextprotocol/server-puppeteer",
      "sub-agents": "Specialized sub-agents - npx -y sub-agents-mcp"
    },
    "api_keys_required": [
      "FIRECRAWL_API_KEY - Get free tier at https://firecrawl.dev (500 credits/mo)",
      "GITHUB_TOKEN - Generate at https://github.com/settings/tokens",
      "BRAVE_API_KEY - Get free tier at https://brave.com/search/api (2,000 queries/mo)"
    ],
    "setup_instructions": [
      "1. Copy env.template to .env",
      "2. Add your API keys to .env",
      "3. Start Cursor or Claude Code",
      "4. MCPs load automatically",
      "5. Test with: 'Use Firecrawl to scrape https://example.com'"
    ]
  }
}
